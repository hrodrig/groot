---
phase: 01-shared-offline-archive-reader
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/arcread/safety_test.go
  - internal/arcread/open_test.go
  - internal/collector/inspect_test.go
  - internal/collector/inspect_golden_test.go
  - internal/collector/inspect.go
  - docs/plan-1.1.0.md
autonomous: true
requirements:
  - READ-01
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Hostile archives fail closed at Open (path traversal, absolute names, symlink/hardlink, oversized member, too many files, decompressed byte bomb) with no extract-to-disk
    - Existing inspect unit/golden tests pass against the arcread-backed InspectArchive
    - Short docs/plan-1.1.0.md stub exists with goal, phase order, and link to .planning/ROADMAP.md
  artifacts:
    - internal/arcread/safety_test.go
    - docs/plan-1.1.0.md
    - internal/collector/inspect.go
  key_links:
    - safety_test hostile fixtures → arcread.Open errors (ErrUnsafePath / ErrUnsupportedType / ErrMemberTooLarge / ErrTooManyFiles / ErrDecompressedCap)
    - inspect_golden_test → DirToTarGz session-prefixed archive → InspectArchive via arcread
---

## Phase Goal

**As a** operator, **I want to** open groot archives offline through one capped typed reader that inspect reuses, **so that** inventory and later analyze share one safe selective-read path.

<objective>
Prove fail-closed hostile-archive behavior, retarget inspect tests, and land the Band 4 plan stub.

Purpose: Complete READ-01 and D-06 so Phase 1 success criteria are observable and Phase 4 has a stub to expand.
Output: safety_test hostile matrix green; inspect tests green on arcread; docs/plan-1.1.0.md stub.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-offline-archive-reader/01-CONTEXT.md
@.planning/phases/01-shared-offline-archive-reader/01-RESEARCH.md
@.planning/phases/01-shared-offline-archive-reader/01-01-PLAN.md
@internal/collector/inspect_test.go
@internal/collector/inspect_golden_test.go
@docs/plan-1.0.0.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Hostile archive fail-closed matrix</name>
  <reversibility rating="reversible">D-03: named cap constants; values can change later without API rewrite.</reversibility>
  <files>internal/arcread/safety_test.go, internal/arcread/open_test.go, internal/arcread/safety.go</files>
  <read_first>internal/arcread/safety.go, internal/arcread/open.go, .planning/phases/01-shared-offline-archive-reader/01-RESEARCH.md</read_first>
  <behavior>
    - Open rejects member names with parent segments or absolute paths via errors.Is(..., ErrUnsafePath)
    - Open rejects TypeSymlink and TypeLink via errors.Is(..., ErrUnsupportedType)
    - Open rejects regular member size above DefaultMaxMemberBytes via ErrMemberTooLarge
    - Open rejects regular file count above DefaultMaxRegularFiles via ErrTooManyFiles (use OpenWithCaps with small test caps for speed)
    - Open rejects decompressed byte total above DefaultMaxDecompressedBytes via ErrDecompressedCap (use OpenWithCaps with small test caps)
    - Truncated gzip and truncated tar return error from Open (fail closed)
    - Successful Open never creates an extract root under the archive path or temp workspace beyond the fixture file itself
  </behavior>
  <action>
    Per D-03 and RESEARCH test plan item 1: add safety_test.go table-driven hostile fixtures built with archive/tar + compress/gzip writers (and OpenWithCaps for count/decompress cases with small Caps so tests stay fast). Cover parent-segment names, absolute names, symlink, hardlink, oversized member, file-count overflow, decompressed-byte overflow, truncated gzip, truncated tar — each must error from Open. Keep production DefaultCaps at 64 MiB / 100_000 / 512 MiB. Document constants with comments naming the defaults. Do not extract archives to disk in tests or production code.
  </action>
  <verify>
    <automated>go test ./internal/arcread/ -count=1 -run 'Hostile|Unsafe|Cap|Symlink|Decompress'</automated>
  </verify>
  <acceptance_criteria>
    - internal/arcread/safety_test.go exists and contains Fail or errors.Is checks for ErrUnsafePath
    - DefaultMaxMemberBytes appears in internal/arcread/safety.go as 67108864 (64 MiB)
    - go test ./internal/arcread/ -count=1 exits 0
  </acceptance_criteria>
  <done>Hostile and oversized archives fail closed at Open with typed sentinel errors; READ-01 covered.</done>
</task>

<task type="auto">
  <name>Retarget inspect tests and stub plan-1.1.0.md</name>
  <reversibility rating="reversible">D-06 stub is short docs; Phase 4 owns full checklist expansion.</reversibility>
  <files>internal/collector/inspect_test.go, internal/collector/inspect_golden_test.go, internal/collector/inspect.go, docs/plan-1.1.0.md</files>
  <read_first>internal/collector/inspect_test.go, internal/collector/inspect_golden_test.go, docs/plan-1.0.0.md, .planning/ROADMAP.md</read_first>
  <action>
    Retarget inspect_test.go and inspect_golden_test.go expectations to the arcread-backed InspectArchive without changing public InspectInfo JSON field names or inventory-only meaning. Keep both bare-prefix and DirToTarGz session-prefixed coverage (LookupSuffix semantics). Confirm inspect.go has no residual direct streaming inventory loop (wrapper calls arcread only).

    Per D-06 write short English stub docs/plan-1.1.0.md containing: Band 4 / v1.1.0 goal (#69 analyze + LLM-ready Markdown), ordered phases (reader → heuristics → LLM Markdown → fixtures/SPEC/release), and an explicit link to .planning/ROADMAP.md. State that full checklist/SPEC lock is Phase 4 (QUAL-03). Do not expand into the full release checklist here.

    Run import-boundary check: go list -f '{{join .Imports "\n"}}' ./internal/arcread must not list Kubernetes client modules. Run go test ./internal/arcread/ ./internal/collector/ -count=1. Require inspect e2e that maps bad gzip to exit code 3 (`go test ./internal/cmd/ -count=1 -run Inspect`).
  </action>
  <verify>
    <automated>go test ./internal/arcread/ ./internal/collector/ ./internal/cmd/ -count=1 -run 'Inspect|Hostile|Unsafe|Cap' ; test "$(go list -f '{{join .Imports "\n"}}' ./internal/arcread | grep -c client-go || true)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - docs/plan-1.1.0.md exists and contains .planning/ROADMAP.md
    - docs/plan-1.1.0.md mentions Phase 1 or Shared Offline Archive Reader
    - internal/collector/inspect.go contains arcread.Open
    - go list -f '{{join .Imports "\n"}}' ./internal/arcread | grep -c client-go returns 0
    - go test ./internal/arcread/ ./internal/collector/ -count=1 exits 0
    - go test ./internal/cmd/ -count=1 -run Inspect exits 0 (bad gzip → exit 3)
  </acceptance_criteria>
  <done>Inspect tests green on shared reader; plan stub landed; import boundary clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic hostile fixtures → Open | Tests prove attacker-controlled tar headers cannot pass caps/path policy |
| Docs stub → operators/maintainers | Informational only; no runtime trust impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | Hostile path/symlink fixtures | high | mitigate | safety_test asserts Open errors; production Open uses IsLocal + type allowlist |
| T-01-07 | Denial of Service | Bomb fixtures (count/size/decompress) | high | mitigate | OpenWithCaps/DefaultCaps fail closed; tests cover all three caps |
| T-01-08 | Tampering | Residual inspect stream loop | medium | mitigate | Retarget tests + confirm inspect.go routes solely through arcread |
| T-01-SC | Tampering | Third-party installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
Before declaring plan complete:
- go test ./internal/arcread/ ./internal/collector/ -count=1
- go list -f '{{join .Imports "\n"}}' ./internal/arcread | grep -c client-go returns 0
- test -f docs/plan-1.1.0.md
- Phase 1 success criteria 1–4 from ROADMAP observable
</verification>

<success_criteria>
- READ-01 hostile matrix green
- Inspect unit/golden tests pass on arcread wrapper
- docs/plan-1.1.0.md stub present with goal, phase order, ROADMAP link
- No Kubernetes client imports in internal/arcread
</success_criteria>

<output>
Create `.planning/phases/01-shared-offline-archive-reader/01-02-SUMMARY.md` when done
</output>
