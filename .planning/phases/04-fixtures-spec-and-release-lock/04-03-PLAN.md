---
phase: 04-fixtures-spec-and-release-lock
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - VERSION
  - README.md
  - CHANGELOG.md
  - contrib/freebsd/Makefile
  - contrib/openbsd/port/Makefile
  - contrib/man/man1/groot.1
  - contrib/man/man1/kubectl-groot.1
autonomous: true
requirements:
  - QUAL-04
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - VERSION is bumped to 1.1.0 (D-08)
    - README.md version badge shows 1.1.0 (D-08)
    - CHANGELOG.md Unreleased analyze bullets are moved into a [1.1.0] section with (1.1.x #69) citations (D-08)
    - FreeBSD/OpenBSD port versions and man pages are synced via make port-freebsd-sync, port-openbsd-sync, man-sync (D-08)
    - make release-check is green before any tag or merge is considered (D-05, D-08)
    - No git tag is created and no merge to main is performed inside executor autonomy (D-09)
  artifacts:
    - VERSION (1.1.0)
    - README.md (version badge)
    - CHANGELOG.md ([1.1.0] section)
    - contrib/freebsd/Makefile (PORTVERSION)
    - contrib/openbsd/port/Makefile (VERSION)
    - contrib/man/man1/groot.1 (.TH version)
    - contrib/man/man1/kubectl-groot.1 (.TH version)
  key_links:
    - VERSION -> README.md badge -> Makefile PORT_VERSION -> man .TH -> CHANGELOG.md [1.1.0]
    - Makefile release-check -> goreleaser check -> fmt-check -> vet -> cover -> security
---

## Phase Goal

**As a** releaser, **I want** the tree versioned and the release checks green, **so that** the only human steps left are merge to main and annotated tag push.

<objective>
Bump `VERSION` to `1.1.0`, synchronize README and packaging metadata, move CHANGELOG Unreleased bullets into `[1.1.0]`, and run `make release-check` until it passes. Do not create or push tags.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-fixtures-spec-and-release-lock/04-CONTEXT.md
@VERSION
@README.md
@CHANGELOG.md
@Makefile
@contrib/freebsd/Makefile
@contrib/openbsd/port/Makefile
@contrib/man/man1/groot.1
@contrib/man/man1/kubectl-groot.1
@.cursor/rules/release-tests.mdc
@.cursor/rules/git-flow.mdc
@.cursor/rules/planning-docs-triad.mdc
</context>

<tasks>

<task type="auto">
  <name>Bump VERSION and sync badges/ports/man pages</name>
  <reversibility rating="reversible">Version metadata changes are reversible by editing the affected files.</reversibility>
  <files>VERSION, README.md, contrib/freebsd/Makefile, contrib/openbsd/port/Makefile, contrib/man/man1/groot.1, contrib/man/man1/kubectl-groot.1</files>
  <read_first>VERSION, README.md, contrib/freebsd/Makefile, contrib/openbsd/port/Makefile, contrib/man/man1/groot.1, contrib/man/man1/kubectl-groot.1, Makefile</read_first>
  <behavior>
    - VERSION contains exactly 1.1.0 (no leading v, no trailing newline issues)
    - README.md version badge URL contains version-1.1.0
    - FreeBSD and OpenBSD port Makefiles reference 1.1.0
    - Man page .TH lines show 1.1.0
  </behavior>
  <action>
    Write VERSION to 1.1.0. Update README.md version badge from 1.0.6 to 1.1.0. Run `make port-freebsd-sync`, `make port-openbsd-sync`, and `make man-sync` to propagate the version. If the Makefile targets are not available, inspect their recipes and apply the same text substitutions manually. Verify each file after the sync commands.
  </action>
  <verify>
    <automated>cat VERSION | grep -q "^1.1.0$" && grep -q "version-1.1.0" README.md && grep -q "1.1.0" contrib/freebsd/Makefile && grep -q "1.1.0" contrib/openbsd/port/Makefile && grep -q "1.1.0" contrib/man/man1/groot.1 && grep -q "1.1.0" contrib/man/man1/kubectl-groot.1</automated>
  </verify>
  <done>
    All version metadata is synchronized to 1.1.0.
  </done>
</task>

<task type="auto">
  <name>CHANGELOG [1.1.0] section and release-check</name>
  <reversibility rating="reversible">CHANGELOG edits and release-check results are reversible.</reversibility>
  <files>CHANGELOG.md</files>
  <read_first>CHANGELOG.md, Makefile, .cursor/rules/release-tests.mdc</read_first>
  <behavior>
    - CHANGELOG.md has a [1.1.0] - 2026-08-10 section containing the Unreleased analyze bullets with (1.1.x #69) citations
    - [Unreleased] is left empty for the next cycle
    - make release-check passes all steps: goreleaser check, fmt-check, vet, cover, security
  </behavior>
  <action>
    Move the existing [Unreleased] Changed and Fixed bullets related to arcread/analyze/inspect into a new [1.1.0] - 2026-08-10 section. Retain the (1.1.x #69) citations. Leave [Unreleased] empty (or remove subsections if none remain). Add a new comparison link at the bottom for [1.1.0] pointing to v1.0.6..v1.1.0.

    Then run `make release-check`. If any step fails, fix the underlying issue and rerun. Common fixes:
    - fmt-check: run `make lint-fix` then retry
    - vet: fix the code issue
    - cover: add tests or adjust coverage; the golden package from 04-01 should help
    - security: govulncheck / gocyclo / grype; grype may fail on transient vulns — evaluate whether they are actionable or note them

    Do not create a git tag or merge to main. If release-check passes, the tree is ready for the human releaser to merge develop → main and push annotated tag v1.1.0.
  </action>
  <verify>
    <automated>grep -q "^## \[1.1.0\]" CHANGELOG.md && make release-check</automated>
  </verify>
  <done>
    CHANGELOG has [1.1.0], version metadata is synced, and make release-check is green.
  </done>
</task>

</tasks>

<verification>
- `cat VERSION` prints `1.1.0`
- README.md badge shows `version-1.1.0`
- CHANGELOG.md contains `[1.1.0] - 2026-08-10` with the moved bullets
- `make release-check` exits 0
</verification>

<success_criteria>
- QUAL-04: CHANGELOG Unreleased bullets moved to [1.1.0] with (1.1.x #69) citations; VERSION/README/ports/man synced; release-check green
- D-08 and D-09 honored: no tag created, no merge to main
</success_criteria>

<output>
Create `.planning/phases/04-fixtures-spec-and-release-lock/04-03-SUMMARY.md` when done
</output>
