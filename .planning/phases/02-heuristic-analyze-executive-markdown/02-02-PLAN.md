---
phase: 02-heuristic-analyze-executive-markdown
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/analyze/heuristics.go
  - internal/analyze/heuristic_oom.go
  - internal/analyze/heuristic_imagepull.go
  - internal/analyze/heuristic_notready.go
  - internal/analyze/heuristic_evicted.go
  - internal/analyze/evidence.go
  - internal/analyze/sort.go
  - internal/analyze/run.go
  - internal/analyze/run_test.go
  - internal/analyze/heuristics_test.go
  - internal/analyze/testdata/oom/
  - internal/analyze/testdata/imagepull/
  - internal/analyze/testdata/notready/
  - internal/analyze/testdata/evicted/
  - internal/analyze/testdata/missing-extras/
autonomous: true
requirements:
  - ANLZ-02
  - ANLZ-03
  - ANLZ-04
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - When archive evidence supports them, analyze surfaces ranked hints for CrashLoopBackOff, OOMKilled, ImagePullBackOff, NotReady, and Evicted
    - Each hint cites member path(s) and short excerpts; language stays hints/hypotheses/open questions
    - Missing extras or older layouts produce explicit insufficient-evidence / member_missing Notes, zero invented hints, exit 0 (not panic, not exit 3)
    - Exit code 137 alone does not emit OOMKilled; Severity sort is deterministic (error before warn before info, then Kind ascending)
  artifacts:
    - internal/analyze/heuristic_oom.go
    - internal/analyze/heuristic_imagepull.go
    - internal/analyze/heuristic_notready.go
    - internal/analyze/heuristic_evicted.go
    - internal/analyze/testdata/missing-extras/
  key_links:
    - loadEvidence → five scanners → sortHints → Report.Hints
    - LookupSuffix miss → Report.Notes → RenderExecutive Notes section
    - OOM scanner requires OOMKilled reason text; 137-only fixture yields Note/open question without OOM hint
---

## Phase Goal

**As a** on-call, **I want to** run offline `groot analyze` and get ranked evidence-backed hints as executive Markdown, **so that** I can investigate incidents without kubeconfig.

<objective>
Complete the five-heuristic set, evidence citations, deterministic ranking, and graceful degrade notes on top of the Plan 01 tracer.

Purpose: Satisfy ANLZ-02/03/04 and locked D-04/D-06/D-08 so Phase 3 can add an LLM renderer over the same Report without new scanners.
Output: Remaining heuristic scanners + testdata matrix under internal/analyze/testdata (not testing/fixtures/archives).
Covers: ANLZ-02, ANLZ-03 (all kinds), ANLZ-04; D-03 enrichment, D-04 full set, D-05 wording, D-06 sort, D-08 degrade.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-heuristic-analyze-executive-markdown/02-CONTEXT.md
@.planning/phases/02-heuristic-analyze-executive-markdown/02-RESEARCH.md
@.planning/phases/02-heuristic-analyze-executive-markdown/02-01-PLAN.md
@.planning/REQUIREMENTS.md
@internal/analyze/run.go
@internal/analyze/report.go
@internal/analyze/evidence.go
@internal/analyze/heuristic_crashloop.go
@internal/collector/collector.go
@internal/collector/unhealthy.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>OOMKilled and ImagePullBackOff heuristics with citations</name>
  <reversibility rating="reversible">Scanner internals stay behind Kind constants and Report; match rules are discretion and can change without CLI contract break.</reversibility>
  <files>internal/analyze/heuristic_oom.go, internal/analyze/heuristic_imagepull.go, internal/analyze/heuristics.go, internal/analyze/evidence.go, internal/analyze/heuristics_test.go, internal/analyze/testdata/oom/, internal/analyze/testdata/imagepull/</files>
  <read_first>internal/analyze/heuristic_crashloop.go, internal/analyze/evidence.go, internal/analyze/report.go, .planning/phases/02-heuristic-analyze-executive-markdown/02-RESEARCH.md</read_first>
  <behavior>
    - oom fixture with terminated/waiting reason OOMKilled (events and/or == pods == JSON) yields KindOOMKilled SeverityError hint with Evidence path + bounded excerpt
    - Separate subtest: exit code 137 without OOMKilled string yields no OOM hint (optional Note/open question only)
    - imagepull fixture with ImagePullBackOff or ErrImagePull backoff context yields KindImagePullBackOff error hint with citations
    - Optional placement/RCA TSV join enriches node/pod_log_file citations when pod identity known; still never treat RCA TSV alone as condition detector (ANLZ-10 deferred)
  </behavior>
  <action>
    Implement OOMKilled and ImagePullBackOff scanners per D-04 and RESEARCH evidence mapping. Prefer extras events + structured pods JSON from resources.txt == pods == sections (minimal local DTOs, encoding/json — no client-go). Use encoding/csv with Comma tab for placement/RCA joins when enriching citations (D-03). Dispatch from heuristics.go so Run invokes all scanners.

    Enforce pitfall guard: OOM requires OOMKilled reason text; do not emit OOM hint from exit 137 alone. ImagePull requires ImagePullBackOff or ErrImagePull with backoff context — not generic Failed alone.

    Every Hint must carry Evidence paths (ANLZ-03) and D-05 wording (hint/hypothesis/open question). Keep excerpts under analyze excerpt cap. Add testdata/oom and testdata/imagepull packed via DirToTarGz; unit tests RED→GREEN.
  </action>
  <verify>
    <automated>go test ./internal/analyze/ -count=1 -run 'OOM|ImagePull|TestRun_'</automated>
  </verify>
  <acceptance_criteria>
    - internal/analyze/heuristic_oom.go and heuristic_imagepull.go exist and are invoked from Run
    - OOMKilled-positive fixture produces Kind OOMKilled hint
    - 137-only fixture does not produce Kind OOMKilled hint
    - ImagePull fixture produces Kind ImagePullBackOff hint with non-empty Evidence
  </acceptance_criteria>
  <done>OOMKilled and ImagePullBackOff hints fire only on supported evidence with path citations.</done>
</task>

<task type="auto" tdd="true">
  <name>NotReady, Evicted, degrade notes, and deterministic ranking</name>
  <reversibility rating="reversible">Additional scanners and Notes codes extend Report without changing CLI flags.</reversibility>
  <files>internal/analyze/heuristic_notready.go, internal/analyze/heuristic_evicted.go, internal/analyze/sort.go, internal/analyze/run.go, internal/analyze/run_test.go, internal/analyze/heuristics_test.go, internal/analyze/testdata/notready/, internal/analyze/testdata/evicted/, internal/analyze/testdata/missing-extras/</files>
  <read_first>internal/analyze/sort.go, internal/analyze/run.go, internal/analyze/report.go, internal/collector/unhealthy.go, .planning/phases/02-heuristic-analyze-executive-markdown/02-RESEARCH.md</read_first>
  <behavior>
    - notready fixture: pods JSON Ready condition status False yields KindNotReady warn hint (structured condition match preferred over bare substring)
    - evicted fixture: Evicted event and/or phase Failed reason Evicted yields KindEvicted hint
    - missing-extras fixture: manifest only → Notes with member_missing/insufficient_evidence, zero invented hints, Run err == nil
    - Multi-hint fixture sorts error before warn before info, then Kind ascending (D-06); stable for goldens
    - Import boundary: go list imports for ./internal/analyze exclude k8s.io/client-go
  </behavior>
  <action>
    Implement NotReady and Evicted scanners per D-04/RESEARCH. Prefer structured pods JSON conditions (type Ready, status False) and Evicted reason/message; secondary events OK. Do not require parity with live CountUnhealthyPods (Pending vs NotReady/Evicted divergence is intentional).

    Complete degrade path per D-08/ANLZ-04: missing events/RCA/resources → Notes, continue, never panic, never invent hints. Older layout without expected members follows the same Note path. Ensure executive MD from Plan 01 already renders Notes (adjust template only if Notes section missing).

    Lock sortHints per D-06. Add matrix tests for notready, evicted, missing-extras; assert all five Kind constants are exercised across the phase testdata set (CrashLoop from Plan 01 plus this plan). Add a small import-boundary test or go list assertion in TestMain/test that internal/analyze does not import client-go.

    Do not add --format=llm, token budgets, omit markers, testing/fixtures/archives corpus, or SPEC analyze section (Phase 3/4). Discretion parsers (contains vs JSON) OK as long as behaviors above pass.
  </action>
  <verify>
    <automated>go test ./internal/analyze/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All five Kind values appear in at least one successful Run test across testdata
    - missing-extras: len(Hints)==0 and len(Notes)&gt;0 and err==nil
    - sort order: SeverityError before SeverityWarn before SeverityInfo for mixed fixtures
    - go list -f '{{join .Imports "\n"}}' ./internal/analyze | grep -c client-go is 0
    - go test ./internal/analyze/ -count=1 exits 0
  </acceptance_criteria>
  <done>Full five-kind heuristic set, citations, degrade Notes, and deterministic ranking ship under internal/analyze.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive member text → heuristic matchers | Untrusted strings; false positives can mislead operators |
| Report.Notes / Hints → executive MD | Degrade path must not crash or invent findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Spoofing | OOM/ImagePull/NotReady/Evicted scanners | medium | mitigate | Require reason/condition evidence; 137-alone and bare Failed rejected; structured Ready==False for NotReady |
| T-02-07 | Information Disclosure | TSV/log excerpt enrichment | medium | mitigate | Excerpt caps; placement join only after pod identity; no deep-logs walk (ANLZ-11 deferred) |
| T-02-08 | Denial of Service | Multi-member loadEvidence | medium | mitigate | Shared caps from Plan 01; no second unbounded ReadMember |
| T-02-09 | Repudiation | Deterministic sort | low | mitigate | Stable severity+Kind sort so goldens and operator diffs are reproducible |
| T-02-SC | Tampering | Third-party installs | low | accept | Zero new modules |
</threat_model>

<verification>
Before declaring plan complete:
- go test ./internal/analyze/ ./internal/cmd/ -count=1
- Confirm testdata covers crashloop, oom, imagepull, notready, evicted, healthy, missing-extras
- Confirm no files under testing/fixtures/archives/ added for this phase
</verification>

<success_criteria>
- ANLZ-02: all five heuristic kinds emit when evidence supports
- ANLZ-03: every hint has path citations + bounded excerpts; D-05 wording
- ANLZ-04: missing-extras degrades with Notes, exit 0, no invented hints
- D-06 sort deterministic; D-08 no panic on older layouts
- Phase 2 ready for Phase 3 LLM renderer over same Report
</success_criteria>

<source_audit>
GOAL | — | Offline analyze ranked executive MD | 02-01+02-02 | COVERED |
REQ | ANLZ-02 | five ranked heuristic kinds | 02-02 | COVERED | CrashLoop from 02-01
REQ | ANLZ-03 | citations + hint language | 02-02 | COVERED | all kinds
REQ | ANLZ-04 | degrade insufficient-evidence | 02-02 | COVERED |
RESEARCH | extras-first + OOM≠137 + structured Ready | 02-02 | COVERED |
CONTEXT | D-04,D-06,D-08 | full heuristic set + sort + degrade | 02-02 | COVERED |
DEFERRED | LLM / QUAL fixtures / ANLZ-10/11 | — | EXCLUDED | Phase 3/4
</source_audit>

<output>
Create `.planning/phases/02-heuristic-analyze-executive-markdown/02-02-SUMMARY.md` when done
</output>
