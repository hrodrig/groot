# Microsoft Teams smoke for `groot notify test`.
# Secrets: export GROOT_NOTIFY_TEAMS_WEBHOOK_URL
# Multiple destinations: separate URLs with ';'

config_version: 1
kubeconfig: ~/.kube/config
output_dir: ./out
collection:
  worker_concurrency: 1
  namespaces: [kube-system]
  include_pod_logs: false
  include_node_details: false
  include_node_logs: false
  include_pod_metrics: false
notify:
  slack: { enabled: false }
  discord: { enabled: false }
  teams:
    enabled: true
    webhook_url: ""
  telegram: { enabled: false }
  generic: { enabled: false }
  pagerduty: { enabled: false }
  email: { enabled: false }
  on_failure:
    enabled: true
    on_abort: true
    min_failed_jobs: 1
