---
phase: 01-shared-offline-archive-reader
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/arcread/open.go
  - internal/arcread/index.go
  - internal/arcread/member.go
  - internal/arcread/manifest.go
  - internal/arcread/safety.go
  - internal/arcread/open_test.go
  - internal/arcread/manifest_test.go
  - internal/collector/inspect.go
  - internal/collector/manifest.go
  - internal/collector/collector.go
autonomous: true
requirements:
  - READ-02
  - READ-03
  - READ-04
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Operator (or tool) opens a valid groot .tar.gz via arcread.Open and gets Members plus typed Manifest without writing extract trees under the filesystem
    - Selective ReadMember returns member bytes by name using ordinal index plus two-pass reopen (not gzip byte seeks)
    - groot inspect inventory path calls arcread only; InspectInfo JSON field names and inventory-only meaning stay SPEC section 13 compatible
    - internal/arcread import graph is stdlib-only (no Kubernetes client libraries)
  artifacts:
    - internal/arcread/open.go
    - internal/arcread/safety.go
    - internal/arcread/manifest.go
    - internal/arcread/member.go
    - internal/arcread/index.go
    - internal/collector/inspect.go
  key_links:
    - collector.InspectArchive → arcread.Open → Members / ManifestRaw / Manifest
    - Archive.ReadMember → reopen path → skip to Ordinal → LimitReader(MaxMemberBytes)
    - writeManifest populates arcread.Manifest JSON tags for archive_layout_version 1
---

## Phase Goal

**As a** operator, **I want to** open groot archives offline through one capped typed reader that inspect reuses, **so that** inventory and later analyze share one safe selective-read path.

<objective>
Ship the shared offline reader package and migrate inspect onto it in the same change set.

Purpose: Unblock Phase 2 analyze and lock D-01…D-05 so every tar consumer shares one fail-closed path.
Output: `internal/arcread` with Open/index/ReadMember/Manifest; thin `InspectArchive`; collect writer uses shared typed Manifest.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-offline-archive-reader/01-CONTEXT.md
@.planning/phases/01-shared-offline-archive-reader/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@internal/collector/inspect.go
@internal/collector/manifest.go
@internal/collector/collector.go
@internal/archive/targz.go
@SPECIFICATIONS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end arcread Open → Manifest → ReadMember → InspectArchive</name>
  <reversibility rating="costly">D-01/D-04: new internal/arcread import graph and inspect sole tar path; moving later rewrites callers.</reversibility>
  <files>internal/arcread/open.go, internal/arcread/index.go, internal/arcread/member.go, internal/arcread/manifest.go, internal/arcread/safety.go, internal/arcread/open_test.go, internal/collector/inspect.go</files>
  <read_first>internal/collector/inspect.go, internal/collector/manifest.go, internal/archive/targz.go, .planning/phases/01-shared-offline-archive-reader/01-RESEARCH.md, .planning/phases/01-shared-offline-archive-reader/01-CONTEXT.md</read_first>
  <behavior>
    - Open of a temp archive with extras/manifest.json (bare prefix and/or DirToTarGz session prefix) succeeds and Members lists regular files only
    - Manifest() decodes typed fields including archive_layout_version == 1; LookupSuffix("extras/manifest.json") finds the member
    - ReadMember of a known regular-file path returns exact bytes via two-pass reopen (ordinal index); no extract directory created
    - InspectArchive(path) returns InspectInfo with Files formatted "%s (%d bytes)", FileCount, ManifestJSON on valid JSON, ParseErr on invalid JSON, and open failures propagated as error
  </behavior>
  <action>
    Create package `internal/arcread` per D-01 with the RESEARCH Concrete API: DefaultMaxMemberBytes=64 MiB (64 shifted left 20), DefaultMaxRegularFiles=100_000, DefaultMaxDecompressedBytes=512 MiB (512 shifted left 20), Caps/DefaultCaps, Open/OpenWithCaps, Archive methods Path/Size/Close/Members/Lookup/LookupSuffix/ReadMember/ReadMemberLimit/Manifest/ManifestRaw, and sentinel errors ErrUnsafePath, ErrUnsupportedType, ErrMemberTooLarge, ErrTooManyFiles, ErrDecompressedCap, ErrMemberNotFound, ErrManifestMissing, ErrManifestParse.

    Implement index-on-open per D-02 using ordinal + two-pass reopen (RESEARCH Pattern 1): Pass 1 streams gzip+tar, validates names with filepath.ToSlash + filepath.IsLocal, allows TypeReg/TypeRegA (index) and TypeDir (no index), rejects symlink/hardlink and other typeflags with ErrUnsupportedType, enforces all three caps while draining bodies via io.LimitReader + decompressed counter (fail closed before returning Archive), records MemberMeta{Name,Size,Typeflag,Ordinal}. Optionally cache extras/manifest.json bytes during Pass 1. ReadMember/ReadMemberLimit reopen from offset 0, recreate gzip+tar, Next until Ordinal, read with LimitReader — never Seek on gzip.Reader.

    Define typed Manifest (+ ManifestCluster, ManifestJobs) in arcread per D-05 mirroring collect captureManifest JSON tags from RESEARCH. Manifest() uses LookupSuffix + decode; raw string path for inspect degrade stays on ParseErr via ManifestRaw/ErrManifestParse.

    Same change set per D-04: rewrite collector.InspectArchive as a thin wrapper over arcread.Open — map Members to Files lines and FileCount, set ManifestJSON from raw when typed decode succeeds, set ParseErr when JSON invalid, preserve InspectInfo struct tags and inventory-only UX (SPEC section 13). Do not leave a second full-archive tar loop in inspect.go.

    Package imports: Go standard library only (archive/tar, compress/gzip, encoding/json, path/filepath, io, os, errors, fmt, strings, and testing in *_test.go). Build fixtures with t.TempDir and/or internal/archive.DirToTarGz.

    Write open_test.go first (RED→GREEN): happy-path Open/Manifest/ReadMember plus InspectArchive inventory assertions for one archive.
  </action>
  <verify>
    <automated>go test ./internal/arcread/ ./internal/collector/ -count=1 -run 'TestOpen|TestManifest|TestReadMember|TestInspectArchive'</automated>
  </verify>
  <acceptance_criteria>
    - internal/arcread/open.go defines func Open(
    - internal/arcread/safety.go defines DefaultMaxMemberBytes
    - internal/collector/inspect.go imports github.com/hrodrig/groot/internal/arcread
    - go test ./internal/arcread/ ./internal/collector/ -count=1 -run 'TestOpen|TestManifest|TestReadMember|TestInspectArchive' exits 0
  </acceptance_criteria>
  <done>Happy path works end-to-end: Open indexes, Manifest typed-decodes, ReadMember returns bytes, InspectArchive is inventory-only via arcread.</done>
</task>

<task type="auto" tdd="true">
  <name>Share typed Manifest with collect writer</name>
  <reversibility rating="costly">D-05: analyze Phase 2 depends on arcread.Manifest fields; collect write path now shares the type.</reversibility>
  <files>internal/arcread/manifest.go, internal/collector/manifest.go, internal/collector/collector.go, internal/arcread/manifest_test.go</files>
  <read_first>internal/collector/manifest.go, internal/collector/collector.go, internal/arcread/manifest.go</read_first>
  <behavior>
    - writeManifest encodes arcread.Manifest (or identical exported fields) with archive_layout_version set to arcread.ArchiveLayoutVersion (1)
    - Round-trip: bytes written by collect-shaped encoder decode via arcread.DecodeManifest / Archive.Manifest without field loss for layout version 1
    - collector.ArchiveLayoutVersion remains usable by existing tests as alias equal to arcread.ArchiveLayoutVersion
  </behavior>
  <action>
    Per D-05 and RESEARCH discretion (share in Phase 1): keep Manifest types in arcread; change writeManifest to populate arcread.Manifest (and nested cluster/jobs types) instead of private captureManifest. Set ArchiveLayoutVersion constant in arcread to 1; make collector.ArchiveLayoutVersion an alias of arcread.ArchiveLayoutVersion or assert equality in tests so call sites keep compiling. Do not change JSON tag names. Add manifest_test.go covering DecodeManifest / Manifest() against a golden JSON blob matching captureManifest tags.
  </action>
  <verify>
    <automated>go test ./internal/arcread/ ./internal/collector/ -count=1 -run 'Manifest|writeManifest|ArchiveLayout'</automated>
  </verify>
  <acceptance_criteria>
    - internal/arcread/manifest.go contains type Manifest struct
    - internal/arcread/manifest.go or open.go defines ArchiveLayoutVersion
    - collector writeManifest path references arcread.Manifest (grep -n 'arcread.Manifest' internal/collector/manifest.go exits 0)
    - go test ./internal/collector/ -count=1 -run 'Manifest|ArchiveLayout' exits 0
  </acceptance_criteria>
  <done>Collect writer and arcread share one Manifest schema for archive_layout_version 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-supplied .tar.gz path → arcread.Open | Untrusted archive bytes cross into process; treat as hostile input |
| arcread → InspectInfo / CLI stdout | Inventory fields only; no heuristic invention |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Open Pass 1 path validation | high | mitigate | Reject non-local names via filepath.IsLocal after ToSlash; reject symlink/hardlink typeflags |
| T-01-02 | Denial of Service | Open Pass 1 body drain | high | mitigate | Enforce 64 MiB / 100k / 512 MiB caps with LimitReader + decompressed counter before returning Archive |
| T-01-03 | Elevation of Privilege | ReadMember / future extract helpers | medium | mitigate | Stream-only API; no extract-to-disk; type policy fail-closed at Open |
| T-01-04 | Information Disclosure | Inspect ManifestJSON | low | accept | Inventory already exposes manifest contents by design (SPEC section 13); no new secret surface |
| T-01-05 | Tampering | Dual tar parsers after migrate | medium | mitigate | Thin InspectArchive only; Plan 02 greps inspect.go free of residual stream loop |
| T-01-SC | Tampering | Third-party installs | low | accept | Phase installs zero new modules (Package Legitimacy N/A) |
</threat_model>

<verification>
Before declaring plan complete:
- go test ./internal/arcread/ ./internal/collector/ -count=1
- go list -f '{{join .Imports "\n"}}' ./internal/arcread shows only stdlib and module-internal non-k8s paths
- InspectArchive still builds Files as "%s (%d bytes)" inventory lines
</verification>

<success_criteria>
- Tracer happy path green for Open/Manifest/ReadMember/InspectArchive
- Typed Manifest shared with collect writer
- Caps constants exist and are enforced during Open (hostile matrix expansion is Plan 02)
- READ-02, READ-03, READ-04 satisfied for the shared-reader slice
</success_criteria>

<output>
Create `.planning/phases/01-shared-offline-archive-reader/01-01-SUMMARY.md` when done
</output>
