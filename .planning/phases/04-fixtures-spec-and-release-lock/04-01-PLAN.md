---
phase: 04-fixtures-spec-and-release-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testing/fixtures/archives/healthy/**
  - testing/fixtures/archives/crashloop/**
  - testing/fixtures/archives/oom/**
  - testing/fixtures/archives/imagepull/**
  - testing/fixtures/archives/missing-manifest/**
  - testing/analyze_golden_test.go
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Committed golden fixtures under testing/fixtures/archives/ cover healthy, CrashLoopBackOff, OOMKilled, ImagePullBackOff, and missing-manifest degrade (QUAL-01, D-01)
    - Fixtures are source trees with session-prefixed layout; tests pack them via archive.DirToTarGz at test time (D-02)
    - Golden tests lock executive Markdown and LLM Markdown for each fixture; expected files live beside fixtures as expected.executive.md and expected.llm.md (D-04)
    - make cover gate (default 80%) still passes after adding the golden package (D-05)
  artifacts:
    - testing/fixtures/archives/healthy/session-*/extras/manifest.json
    - testing/fixtures/archives/healthy/session-*/extras/all-cluster-events.log
    - testing/fixtures/archives/healthy/session-*/default/resources.txt
    - testing/fixtures/archives/crashloop/session-*/extras/manifest.json
    - testing/fixtures/archives/crashloop/session-*/extras/all-cluster-events.log
    - testing/fixtures/archives/crashloop/session-*/default/resources.txt
    - testing/fixtures/archives/oom/session-*/extras/manifest.json
    - testing/fixtures/archives/oom/session-*/extras/all-cluster-events.log
    - testing/fixtures/archives/oom/session-*/extras/all-pod-node-placement.tsv
    - testing/fixtures/archives/oom/session-*/default/resources.txt
    - testing/fixtures/archives/imagepull/session-*/extras/manifest.json
    - testing/fixtures/archives/imagepull/session-*/extras/all-cluster-events.log
    - testing/fixtures/archives/imagepull/session-*/default/resources.txt
    - testing/fixtures/archives/missing-manifest/session-*/extras/all-cluster-events.log
    - testing/fixtures/archives/missing-manifest/session-*/default/resources.txt
    - testing/fixtures/archives/*/expected.executive.md
    - testing/fixtures/archives/*/expected.llm.md
    - testing/analyze_golden_test.go
  key_links:
    - testing/analyze_golden_test.go -> archive.DirToTarGz -> arcread.Open -> analyze.Run -> RenderExecutive/RenderLLM -> go-cmp / os.ReadFile golden compare
---

## Phase Goal

**As a** maintainer, **I want** a committed golden corpus and CI-level golden tests for `groot analyze`, **so that** heuristic or rendering regressions fail the build instead of silently changing output.

<objective>
Ship the QUAL-01 fixture corpus and QUAL-02 golden tests under `testing/`. Reuse/adapt existing `internal/analyze/testdata` patterns, pack fixtures at test time via `archive.DirToTarGz`, and lock both executive and LLM Markdown outputs for five fixture scenarios.

Purpose: close QUAL-01 and QUAL-02 in one wave so the release-lock wave can trust `make cover` and `make release-check`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-fixtures-spec-and-release-lock/04-CONTEXT.md
@internal/analyze/report.go
@internal/analyze/run.go
@internal/analyze/evidence.go
@internal/analyze/render_executive.go
@internal/analyze/render_llm.go
@internal/analyze/heuristics.go
@internal/analyze/heuristic_crashloop.go
@internal/analyze/heuristic_oom.go
@internal/analyze/heuristic_imagepull.go
@internal/analyze/testdata/healthy/extras/manifest.json
@internal/analyze/testdata/crashloop/extras/manifest.json
@internal/analyze/testdata/crashloop/extras/all-cluster-events.log
@internal/analyze/testdata/oom/extras/manifest.json
@internal/analyze/testdata/oom/extras/all-cluster-events.log
@internal/analyze/testdata/oom/extras/all-pod-node-placement.tsv
@internal/analyze/testdata/imagepull/extras/manifest.json
@internal/analyze/testdata/imagepull/extras/all-cluster-events.log
@internal/analyze/testdata/missing-extras/extras/manifest.json
@internal/archive/targz.go
@internal/arcread/open.go
@internal/arcread/manifest.go
@internal/arcread/member.go
@internal/arcread/index.go
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Fixture source trees under testing/fixtures/archives/</name>
  <reversibility rating="reversible">Adding testdata directories is reversible; deleting them removes the corpus and tests.</reversibility>
  <files>testing/fixtures/archives/healthy/**, testing/fixtures/archives/crashloop/**, testing/fixtures/archives/oom/**, testing/fixtures/archives/imagepull/**, testing/fixtures/archives/missing-manifest/**</files>
  <read_first>internal/analyze/testdata/healthy/extras/manifest.json, internal/analyze/testdata/crashloop/extras/manifest.json, internal/analyze/testdata/crashloop/extras/all-cluster-events.log, internal/analyze/testdata/oom/extras/manifest.json, internal/analyze/testdata/oom/extras/all-cluster-events.log, internal/analyze/testdata/oom/extras/all-pod-node-placement.tsv, internal/analyze/testdata/imagepull/extras/manifest.json, internal/analyze/testdata/imagepull/extras/all-cluster-events.log, internal/analyze/testdata/missing-extras/extras/manifest.json, internal/arcread/manifest.go, internal/arcread/index.go, internal/archive/targz.go</read_first>
  <behavior>
    - Each fixture is a directory under testing/fixtures/archives/&lt;name&gt;/session-YYYY-MM-DD-HHMMSS/ with the same relative layout a real groot archive uses.
    - healthy: manifest.json + all-cluster-events.log with only Normal/Scheduled events + default/resources.txt with a Running/Ready pod.
    - crashloop: manifest.json + all-cluster-events.log containing "Back-off restarting failed container ... CrashLoopBackOff" + default/resources.txt with a non-ready pod.
    - oom: manifest.json + all-cluster-events.log containing "OOMKilled" + default/resources.txt with a pod whose container status has terminated.reason=OOMKilled + extras/all-pod-node-placement.tsv with a matching pod row.
    - imagepull: manifest.json + all-cluster-events.log containing "ImagePullBackOff" + default/resources.txt with a pod whose container status has waiting.reason=ImagePullBackOff.
    - missing-manifest: default/resources.txt + all-cluster-events.log but no extras/manifest.json so analyze.Run emits a member_missing note.
    - manifest.json fields include archive_layout_version=1, run_id, archive_sha256, archive_basename, session_base, file_prefix, collected_at, cluster, jobs, paths.
  </behavior>
  <action>
    Create the five fixture directories. Reuse the exact JSON/log shapes from internal/analyze/testdata where they already match the scenario; adapt only when needed for a cleaner golden output. Keep fixture files small (no log dumps) so the default LLM render path does not truncate. Use a deterministic timestamp like 2026-08-10-120000 and pod names like demo-0, web-0, oom-0, pull-0 so golden outputs are stable.

    For missing-manifest, do not create extras/manifest.json. Verify the layout still passes arcread safety checks (it will; missing manifest is a degrade, not a cap failure).

    Add a short README at testing/fixtures/archives/README.md documenting the corpus, how to regenerate the .tar.gz (archive.DirToTarGz), and how to update goldens (UPDATE_GOLDEN=1).
  </action>
  <verify>
    <automated>go test ./testing/... -count=1 -run TestFixtureCorpusExists</automated>
  </verify>
  <done>
    Five source-tree fixtures exist under testing/fixtures/archives/ with deterministic, minimal content that exercises each heuristic and the degrade path.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Golden tests for executive and LLM Markdown</name>
  <precondition>Fixture source trees exist and can be packed into .tar.gz by archive.DirToTarGz.</precondition>
  <files>testing/analyze_golden_test.go, testing/fixtures/archives/*/expected.executive.md, testing/fixtures/archives/*/expected.llm.md</files>
  <read_first>internal/analyze/render_executive.go, internal/analyze/render_llm.go, internal/analyze/run.go, internal/arcread/open.go, internal/archive/targz.go, internal/cmd/analyze_test.go</read_first>
  <behavior>
    - TestAnalyzeGolden/&lt;fixture&gt; packs the source tree into a temp .tar.gz, opens it with arcread.Open, runs analyze.Run, renders both executive and LLM Markdown, and compares each to the corresponding expected.*.md golden file.
    - Running with UPDATE_GOLDEN=1 overwrites the golden files with current output.
    - Golden comparisons ignore whitespace differences that are not material (trim trailing whitespace; normalize final newline) or use go-cmp with a normalizer.
    - The test package does not import client-go or touch a live cluster.
    - make cover still passes with the new package included (merged coverage >= 80%).
  </behavior>
  <action>
    Create testing/analyze_golden_test.go. Define a helper packFixture(t, name) string that calls archive.DirToTarGz on testing/fixtures/archives/&lt;name&gt;/&lt;session-dir&gt; and returns the temp archive path. Define runGolden(t, name, format string) that opens the archive, runs analyze.Run, and returns the rendered string. Use a table-driven test over []struct{ name string; format string; golden string } for both executive and llm formats.

    Use github.com/google/go-cmp/cmp if already in go.mod, or strings equality with trailing-whitespace normalization. Check go.mod first. If go-cmp is not present, use standard string comparison after normalizing with strings.TrimRight on each line plus final newline; do not add a new dependency just for this test.

    Generate the initial golden files by running the tests with UPDATE_GOLDEN=1, then review them manually to ensure the output contains the expected hint titles, evidence citations, and secrets warning for LLM. Fix any fixture content that produces unstable output (e.g., timestamps in pod names) before committing goldens.

    Ensure the golden package has enough unit coverage to not drop the merged gate. If coverage dips, add a small pure unit test inside testing/ covering the pack helper error path or the normalize function.
  </action>
  <verify>
    <automated>go test ./testing/... -count=1 -race -run TestAnalyzeGolden</automated>
    <automated>make cover</automated>
  </verify>
  <done>
    Golden tests lock executive and LLM output for all five fixtures; make cover passes; CI can fail on output drift.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture source tree → test .tar.gz | Local deterministic content generated by the test; no untrusted input |
| Golden file → human review | Goldens are committed text; a reviewer can inspect expected output |
| Test runner → CI | Tests assert output stability; a regression changes the golden diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Fixture pod names / events are synthetic; no real cluster data | low | accept | Use synthetic pod names and no live cluster credentials |
| T-04-02 | Denial of Service | Tests create temp .tar.gz files | low | accept | Use t.TempDir; files are small and cleaned up automatically |
| T-04-03 | Tampering | Golden drift from intentional heuristic change | low | mitigate | UPDATE_GOLDEN workflow; PR review of golden diffs |
| T-04-SC | Supply Chain | No new dependencies | low | accept | Use existing go.mod packages only |
</threat_model>

<verification>
- `go test ./testing/... -count=1 -race`
- `make cover` (merged coverage >= 80%)
- `go test ./internal/analyze/... ./internal/cmd/... -count=1 -race` (existing tests still pass)
</verification>

<success_criteria>
- QUAL-01: Fixture corpus under `testing/fixtures/archives/` covers healthy, crashloop, oom, imagepull, missing-manifest
- QUAL-02: Golden tests lock executive + LLM Markdown for all fixtures; `make cover` passes
- D-01..D-05 honored; no new fixtures beyond the minimum unless cheap
</success_criteria>

<output>
Create `.planning/phases/04-fixtures-spec-and-release-lock/04-01-SUMMARY.md` when done
</output>
