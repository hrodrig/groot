---
phase: 03-llm-ready-markdown-packaging
plan: 01
subsystem: analyze
tags: [llm, markdown, render, budget, cobra, offline]

requires:
  - phase: 02-heuristic-analyze-executive-markdown
    provides: format-agnostic analyze.Report, RenderExecutive twin pattern, analyze CLI --output switch
provides:
  - RenderLLM(Report) pure LLM-ready Markdown renderer
  - DefaultLLMBudgetBytes=32768 with priority shrink + head/tail omit markers
  - CLI --output llm path with secrets warning and distinct footer
affects: [04-spec-fixtures, paste packaging, Phase 4 SPEC lock]

actuals:
  tokens: 8100
  tasks: 2
  commits: 0

tech-stack:
  added: []
  patterns:
    - Twin renderer over one Report (RenderExecutive / RenderLLM / JSON)
    - Priority shrink view-model then head/tail omit on findings body
    - Constant-only byte budget (no tiktoken, no --budget-bytes)

key-files:
  created:
    - internal/analyze/render_llm.go
    - internal/analyze/render_llm_test.go
    - internal/analyze/testdata/llm_crashloop.golden.md
  modified:
    - internal/cmd/analyze.go
    - internal/cmd/analyze_test.go

key-decisions:
  - "DefaultLLMBudgetBytes = 32 KiB (32768); constant-only, no CLI budget flag"
  - "llmExcerptMaxRunes = 256 at render time; Report excerpts unchanged for JSON"
  - "Omit marker locked as … [omitted N bytes] … with N = middle UTF-8 bytes removed"
  - "Keep ≥1 hint before head/tail so omit markers remain visible under forced tiny budgets"
  - "Golden clears ArchivePath (temp pack path is not stable)"

patterns-established:
  - "Pattern: pure Report renderer — no arcread import, no Run call inside RenderLLM"
  - "Pattern: assemble prefix + findings + suffix; never drop Instructions / Secrets warning / footer"

requirements-completed: [LLM-01, LLM-02, LLM-03]

coverage:
  - id: D1
    description: RenderLLM emits D-06-ordered paste Markdown with system-prompt framing, findings, secrets warning, and llm footer
    requirement: LLM-01
    verification:
      - kind: unit
        ref: internal/analyze/render_llm_test.go#TestRenderLLM_SyntheticPureNoArchive
        status: pass
      - kind: unit
        ref: internal/analyze/render_llm_test.go#TestRenderLLM_CrashLoopGolden
        status: pass
      - kind: e2e
        ref: internal/cmd/analyze_test.go#TestAnalyze_LLMOutput
        status: pass
    human_judgment: false
  - id: D2
    description: Hard 32 KiB ceiling with priority shrink and visible head/tail omit markers
    requirement: LLM-02
    verification:
      - kind: unit
        ref: internal/analyze/render_llm_test.go#TestRenderLLM_FatReportRespectsCeiling
        status: pass
      - kind: unit
        ref: internal/analyze/render_llm_test.go#TestRenderLLM_TinyBudgetOmitMarker
        status: pass
    human_judgment: false
  - id: D3
    description: Bounded excerpts + secrets warning; no vendor-safe paste claims; no #45 redaction engine
    requirement: LLM-03
    verification:
      - kind: unit
        ref: internal/analyze/render_llm_test.go#TestRenderLLM_SyntheticPureNoArchive
        status: pass
      - kind: e2e
        ref: internal/cmd/analyze_test.go#TestAnalyze_LLMOutput
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-08-10
status: complete
---

# Phase 3 Plan 01: LLM-Ready Markdown Packaging Summary

**Budgeted `RenderLLM(Report)` paste pack wired as `--output llm`, with 32 KiB ceiling, visible omit markers, and an explicit secrets warning — pure over the Phase 2 Report.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-08-11T00:08:25Z
- **Completed:** 2026-08-11T00:12:30Z
- **Tasks:** 2/2
- **Files modified:** 5 (3 created, 2 modified) + SUMMARY

## Accomplishments

- Shipped `RenderLLM` / `renderLLM(budget)` with D-06 section order, English system-prompt framing, and footer `generated by groot analyze --output llm`
- Enforced `DefaultLLMBudgetBytes = 32768` via priority shrink then rune-safe `headTailOmit` (`… [omitted N bytes] …`)
- Wired CLI `--output llm`; help/invalid-output wording includes `llm`; text/json paths unchanged
- Locked crashloop golden + CLI e2e; clip excerpts to 256 runes at render time; secrets warning denies ChatGPT-safe claims

## Task Commits

Per operator instruction, **no git commits** were created. Work is staged for human-approved commit.

1. **Task 1: End-to-end RenderLLM → --output llm → stdout** — staged (uncommitted)
2. **Task 2: Budget ceiling, head/tail omit, priority shrink** — staged (uncommitted)

**Plan metadata:** SUMMARY written; commit deferred for human approval

## Files Created/Modified

- `internal/analyze/render_llm.go` — `RenderLLM`, budget constants, shrink + `headTailOmit`
- `internal/analyze/render_llm_test.go` — purity, golden, ceiling, omit, framing overflow, import boundary
- `internal/analyze/testdata/llm_crashloop.golden.md` — crashloop LLM golden
- `internal/cmd/analyze.go` — `--output llm` case + help text
- `internal/cmd/analyze_test.go` — `TestAnalyze_LLMOutput` + help/invalid coverage

## Decisions Made

- Constant-only 32 KiB budget (no `--budget-bytes`, no tiktoken)
- Keep at least one hint before last-resort head/tail so omit markers stay visible under forced tiny budgets
- Clear `ArchivePath` before golden compare (temp tar paths are unstable)
- Zero new Go module dependencies

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] Keep ≥1 hint before head/tail omit**
- **Found during:** Task 2 (tiny-budget omit marker test)
- **Issue:** Aggressive shrink dropped all hints, so outputs fit under tiny budgets without ever inserting omit markers
- **Fix:** `shrinkLLMView` drops trailing hints only while `len(hints) > 1`, then `headTailOmit` runs on the findings body
- **Files modified:** `internal/analyze/render_llm.go`
- **Commit:** uncommitted (human approval pending)

**2. [Rule 1 - Bug] Golden instability from temp ArchivePath**
- **Found during:** Task 1 (golden compare)
- **Issue:** `Run` stamped a TempDir archive path into Report, breaking full-file golden compare across runs
- **Fix:** Clear `rep.ArchivePath` in `TestRenderLLM_CrashLoopGolden` before render
- **Files modified:** `internal/analyze/render_llm_test.go`
- **Commit:** uncommitted (human approval pending)

## Known Stubs

None.

## Threat Flags

None beyond plan threat model (T-03-01..T-03-04 mitigated in renderer; T-03-SC N/A — no new packages).

## Self-Check: PASSED

- FOUND: `internal/analyze/render_llm.go`
- FOUND: `internal/analyze/render_llm_test.go`
- FOUND: `internal/analyze/testdata/llm_crashloop.golden.md`
- FOUND: CLI `case "llm"` in `internal/cmd/analyze.go`
- VERIFY: `go test ./internal/analyze/ ./internal/cmd/ -count=1 -run 'LLM|RenderLLM|Analyze_Invalid|Analyze_CrashLoop|Analyze_JSON'` → ok
- VERIFY: `go test ./internal/analyze/ -count=1 -run 'LLM|RenderLLM' -race` → ok
- COMMITS: none (intentionally deferred)
