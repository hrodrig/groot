---
phase: 02-heuristic-analyze-executive-markdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/analyze/report.go
  - internal/analyze/run.go
  - internal/analyze/evidence.go
  - internal/analyze/heuristics.go
  - internal/analyze/heuristic_crashloop.go
  - internal/analyze/sort.go
  - internal/analyze/render_executive.go
  - internal/analyze/run_test.go
  - internal/analyze/render_executive_test.go
  - internal/analyze/testdata/crashloop/
  - internal/analyze/testdata/healthy/
  - internal/cmd/analyze.go
  - internal/cmd/analyze_test.go
  - internal/cmd/root.go
autonomous: true
requirements:
  - ANLZ-01
  - ANLZ-03
  - ANLZ-05
  - ANLZ-06
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Operator runs groot analyze on a local .tar.gz with no kubeconfig and gets executive Markdown on stdout by default
    - CrashLoopBackOff evidence produces one ranked hint with archive member path citation and a bounded excerpt
    - --output json prints the same Report shape; archive open/read failures map to exit code 3; zero-hint success exits 0 with healthy/empty summary
    - Executive Markdown header includes run_id and archive_sha256 when Manifest provides them; wording uses hints/hypotheses/open questions language per D-05
  artifacts:
    - internal/analyze/run.go
    - internal/analyze/report.go
    - internal/analyze/render_executive.go
    - internal/analyze/heuristic_crashloop.go
    - internal/cmd/analyze.go
  key_links:
    - newAnalyzeCmd → arcread.Open → analyze.Run → RenderExecutive or json.MarshalIndent → stdout
    - analyze.Run → loadEvidence LookupSuffix/ReadMemberLimit → CrashLoop heuristic → sorted Report.Hints
    - open/read errors → NewExitErrorf(ExitCollectAborted) → ExitCodeOf == 3
---

## Phase Goal

**As a** on-call, **I want to** run offline `groot analyze` and get ranked evidence-backed hints as executive Markdown, **so that** I can investigate incidents without kubeconfig.

<objective>
Ship the end-to-end offline analyze path: format-agnostic Report, CrashLoopBackOff heuristic, executive Markdown default, JSON output, and exit-code 3 on archive I/O failure.

Purpose: Prove arcread → analyze.Run → CLI rendering before expanding the remaining four heuristic kinds (Plan 02).
Output: `internal/analyze` package + `groot analyze` Cobra command registered on root.
Covers: ANLZ-01, ANLZ-03 (CrashLoop path), ANLZ-05, ANLZ-06; D-01, D-02, D-03 (extras-first load), D-04 (CrashLoop only this plan), D-05, D-06 (sort helpers), D-07.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-heuristic-analyze-executive-markdown/02-CONTEXT.md
@.planning/phases/02-heuristic-analyze-executive-markdown/02-RESEARCH.md
@.planning/REQUIREMENTS.md
@internal/arcread/open.go
@internal/arcread/manifest.go
@internal/arcread/index.go
@internal/arcread/member.go
@internal/cmd/validate.go
@internal/cmd/exitcode.go
@internal/cmd/root.go
@internal/archive/targz.go
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end analyze.Run CrashLoop → executive Markdown → CLI</name>
  <reversibility rating="costly">D-01: new internal/analyze package plus newAnalyzeCmd on root; later relocation rewrites import graph and CLI surface.</reversibility>
  <files>internal/analyze/report.go, internal/analyze/run.go, internal/analyze/evidence.go, internal/analyze/heuristics.go, internal/analyze/heuristic_crashloop.go, internal/analyze/sort.go, internal/analyze/render_executive.go, internal/analyze/run_test.go, internal/analyze/testdata/crashloop/, internal/cmd/analyze.go, internal/cmd/root.go</files>
  <read_first>internal/cmd/validate.go, internal/cmd/exitcode.go, internal/cmd/root.go, internal/arcread/manifest.go, internal/arcread/index.go, internal/arcread/member.go, .planning/phases/02-heuristic-analyze-executive-markdown/02-CONTEXT.md, .planning/phases/02-heuristic-analyze-executive-markdown/02-RESEARCH.md</read_first>
  <behavior>
    - Pack tiny crashloop tree (extras/manifest.json with run_id + archive_sha256, extras/all-cluster-events.log or warning-events containing CrashLoopBackOff / Back-off restarting failed container) via archive.DirToTarGz under t.TempDir / committed testdata/crashloop source
    - analyze.Run(arc) returns Report with exactly one Hint KindCrashLoopBackOff, SeverityError, non-empty Evidence path (LookupSuffix member Name) and bounded Excerpt; no client-go import in package
    - RenderExecutive(report) Markdown includes title groot analyze, disclaimer that findings are hints/hypotheses, header run_id and archive_sha256 when set, and the CrashLoop hint section
    - groot analyze &lt;archive&gt; (default --output text) writes that Markdown to stdout and returns nil (exit 0)
  </behavior>
  <action>
    Create package internal/analyze per D-01 with RESEARCH API sketch: Severity (error &gt; warn &gt; info), Kind constants for all five kinds (CrashLoopBackOff, OOMKilled, ImagePullBackOff, NotReady, Evicted) even if only CrashLoop scanner is implemented this plan, Evidence/Hint/Note/Report structs with snake_case JSON tags, Run(*arcread.Archive) (Report, error), RenderExecutive(Report) (string, error).

    Implement extras-first loadEvidence per D-03 using LookupSuffix + ReadMemberLimit with analyze-local caps (events/TSV/resources ~2 MiB, log excerpt ~4 KiB). Prefer extras/all-cluster-events.log and extras/warning-events.log; do not walk pod logs unboundedly. Missing optional members append Note codes such as member_missing or insufficient_evidence and continue — Run returns error only for catastrophic I/O, not missing extras (D-07/D-08 foundation).

    Implement CrashLoopBackOff heuristic only (D-04 partial): emit Hint when events or pods JSON waiting reason supports CrashLoopBackOff; cite archive member path; clip excerpt. Use hint/hypothesis/open-question language per D-05 (no definitive diagnosis verbs). Sort helpers per D-06 (severity rank then Kind) even with one hint.

    Render executive Markdown via text/template (Option missingkey=error) per RESEARCH/CONTEXT discretion — sections: title, archive header (path, run_id, archive_sha256, layout version when present), disclaimer, summary, ranked hints, notes, footer generated by groot analyze. No LLM framing, budgets, or omit markers (Phase 3).

    Add thin newAnalyzeCmd in internal/cmd/analyze.go mirroring newInspectCmd: ExactArgs(1), --output text|json default text (D-02), arcread.Open, defer Close, analyze.Run, render text via RenderExecutive or json.MarshalIndent. Register with rootCmd.AddCommand in root.go. Map open/Run failures with NewExitErrorf(ExitCollectAborted, ...) per D-07.

    Write failing end-to-end tests first (RED→GREEN): Run + RenderExecutive against crashloop fixture; optional thin CLI Execute asserting stdout contains CrashLoopBackOff and exit 0.
  </action>
  <verify>
    <automated>go test ./internal/analyze/ ./internal/cmd/ -count=1 -run 'TestRun|TestRenderExecutive|TestAnalyze|CrashLoop'</automated>
  </verify>
  <acceptance_criteria>
    - internal/analyze/run.go defines func Run(
    - internal/analyze/render_executive.go defines func RenderExecutive(
    - internal/cmd/analyze.go defines func newAnalyzeCmd(
    - internal/cmd/root.go registers newAnalyzeCmd
    - go test ./internal/analyze/ ./internal/cmd/ -count=1 -run 'TestRun|TestRenderExecutive|TestAnalyze|CrashLoop' exits 0
  </acceptance_criteria>
  <done>Operator can run groot analyze on a CrashLoop fixture and get executive Markdown with a cited CrashLoopBackOff hint end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>JSON output, exit 3, and healthy empty summary</name>
  <reversibility rating="reversible">CLI output branches and exit mapping stay behind newAnalyzeCmd; schema field names remain discretion until Phase 4 SPEC lock.</reversibility>
  <files>internal/analyze/run.go, internal/analyze/render_executive.go, internal/analyze/run_test.go, internal/analyze/render_executive_test.go, internal/analyze/testdata/healthy/, internal/cmd/analyze.go, internal/cmd/analyze_test.go</files>
  <read_first>internal/cmd/validate_inspect_e2e_test.go, internal/cmd/exitcode.go, internal/cmd/analyze.go, internal/analyze/report.go</read_first>
  <behavior>
    - --output json emits Report JSON with hints array, notes, summary, run_id, archive_sha256 when present; Severity marshals as strings error|warn|info (discretion A2)
    - arcread.Open failure (missing/corrupt archive) returns ExitCollectAborted (3) via ExitCodeOf
    - healthy fixture (manifest + benign/empty events, no heuristic strings) yields zero hints, exit 0, Summary healthy/empty one-liner in both text and JSON
    - invalid --output value rejected with clear error (non-3 unless open failed)
  </behavior>
  <action>
    Complete renderAnalyze for text|json per D-02/ANLZ-06. Ensure Manifest-derived RunID/ArchiveSHA256/ArchiveLayoutVersion populate Report for ANLZ-05/D-05 header. Document exit behavior in command Long string aligned with inspect (0 success including zero hints; 3 archive read failure) per D-07 — do not invent LLM format flag.

    Add testdata/healthy and CLI/unit tests covering JSON shape, ExitCodeOf==3 on bad path, and zero-hint executive summary. Keep fixtures under internal/analyze/testdata only (not testing/fixtures/archives — Phase 4 QUAL-01).

    Keep Report free of Markdown strings inside heuristics so Phase 3 can add a second renderer over the same model.
  </action>
  <verify>
    <automated>go test ./internal/analyze/ ./internal/cmd/ -count=1 -run 'TestRun|TestRender|TestAnalyze|Healthy|JSON|Exit'</automated>
  </verify>
  <acceptance_criteria>
    - analyze command Long or help mentions exit code 3 for archive read failure
    - go test ./internal/cmd/ -count=1 -run 'Analyze.*Exit|Exit.*Analyze|TestAnalyze' covers ExitCollectAborted path
    - healthy path: len(Hints)==0 and exit 0
    - --output json round-trips Report fields used by goldens
  </acceptance_criteria>
  <done>JSON output works; archive I/O failures exit 3; healthy archives exit 0 with empty/healthy summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-supplied .tar.gz → arcread.Open → analyze.Run | Untrusted archive bytes; analyze must not bypass arcread caps |
| Report → stdout (Markdown/JSON) | Operator-visible excerpts from archive members; keep bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | loadEvidence ReadMemberLimit | high | mitigate | Analyze-local caps (~2 MiB events/TSV, ~4 KiB log excerpts); never raw os.ReadFile of unpacked trees |
| T-02-02 | Information Disclosure | Evidence.Excerpt / executive MD | medium | mitigate | Hard excerpt clip; extras-first; no full pod log dump (Phase 3 owns paste budgets) |
| T-02-03 | Spoofing | Hint titles/summaries | medium | mitigate | D-05 hint/hypothesis language; require Evidence paths before emitting Hint |
| T-02-04 | Tampering | Member path citations | medium | mitigate | Cite LookupSuffix Name only; do not synthesize paths |
| T-02-05 | Elevation of Privilege | internal/analyze imports | high | mitigate | Forbid client-go / kubeloader imports; arcread + stdlib only |
| T-02-SC | Tampering | Third-party installs | low | accept | Phase installs zero new modules (Package Legitimacy N/A) |
</threat_model>

<verification>
Before declaring plan complete:
- go test ./internal/analyze/ ./internal/cmd/ -count=1
- go list -f '{{join .Imports "\n"}}' ./internal/analyze shows no k8s.io/client-go
- groot analyze --help lists analyze and --output text|json
</verification>

<success_criteria>
- Tracer path green: CrashLoop fixture → Run → executive MD → CLI exit 0
- JSON + exit 3 + healthy empty summary green
- ANLZ-01, ANLZ-05, ANLZ-06 satisfied; ANLZ-03 satisfied for CrashLoop slice
- Remaining four heuristic kinds deferred only to Plan 02-02 (same phase), not later milestones
</success_criteria>

<source_audit>
GOAL | — | Offline analyze ranked executive MD | 02-01 | COVERED | Tracer + CLI
REQ | ANLZ-01 | analyze offline no kubeconfig | 02-01 | COVERED |
REQ | ANLZ-03 | citations + hint language | 02-01 | COVERED | CrashLoop path
REQ | ANLZ-05 | executive MD + run_id/sha | 02-01 | COVERED |
REQ | ANLZ-06 | JSON + exit 3 | 02-01 | COVERED |
CONTEXT | D-01..D-03,D-05..D-07 | package/CLI/evidence/wording/exits | 02-01 | COVERED |
CONTEXT | D-04 | five kinds | 02-01 | PARTIAL | CrashLoop only; 02-02 completes
</source_audit>

<output>
Create `.planning/phases/02-heuristic-analyze-executive-markdown/02-01-SUMMARY.md` when done
</output>
